import React from "react";

// styles
import "./MenuNavbar.scss";

const MenuNavbar = () => {
  return (
    <div className="MenuNavbar">
      <button className="MenuButton">Like</button>
      <button className="MenuButton">Super Like</button>
    </div>
  );
};

export default MenuNavbar;
